
<% @organisations.each do |org| %>
<div class="row">
  <div class="col-md-12">
    <h2 class="organisation-title"><%= link_to org.name, org %></h2>
    <i><%= org.address %></i>
    <p><%= org.description %></p>
  </div>
</div>
<% end %>