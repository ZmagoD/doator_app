<div class="row">
  <div class="col-md-6 col-md-offset-3 well">
    <h2>Registracija</h2>
    
    <%= simple_form_for [@colectionloc, @organisation] do |f| %>
      <%= f.input :type, label: "Naziv" %> 
      <%= f.input :goods, label: "Naziv" %> 
      <%= f.input :description, label: "Naziv" %> 
      <%= f.submit "Potrdi", class: "btn btn-default btn-lg" %>
    <% end %>
  </div>
</div>